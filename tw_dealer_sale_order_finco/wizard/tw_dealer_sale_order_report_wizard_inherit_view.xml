<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="tw_dealer_sale_order_report_finco_wizard_view" model="ir.ui.view">
            <field name="name">tw.dealer.sale.order.report.finco.wizard</field>
            <field name="model">tw.dealer.sale.order</field>
            <field name="inherit_id" ref="tw_dealer_sale_order.tw_dealer_sale_order_report_wizard_view" />
            <field name="arch" type="xml">

                <button name="action_print_report_dp_po" position="attributes">
                    <attribute name="invisible">state not in ('sale', 'done') or finco_id == False</attribute>
                </button>

                <button name="action_pelunasan_leasing_bank_wizard_view" position="attributes">
                    <attribute name="invisible">state not in ('sale', 'done') or finco_id == False</attribute>
                </button>

            </field>
        </record>

    </data>
</odoo>