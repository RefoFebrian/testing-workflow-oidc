<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="tw_stock_extras_monitoring_expedition_apps_form_view" model="ir.ui.view">
            <field name="name">tw.stock.extras.monitoring.expedition.apps.form.view</field>
            <field name="model">stock.picking</field>
            <field name="inherit_id" ref="tw_expedition_apps.tw_monitoring_expedition_apps_form_view"/>
            <field name="arch" type="xml">
                <field name="move_ids_without_extras" position="before">
                    <label for="move_ids_without_extras" string="UNIT" invisible="not extras_move_ids"/>
                </field>
                <field name="move_ids_without_extras" position="after">
                    <label for="extras_move_ids" string="EXTRAS" invisible="not extras_move_ids"/>
                    <field name="extras_move_ids"
                        widget="stock_move_one2many"
                        readonly="1"
                        invisible="not extras_move_ids"
                        context="{'default_company_id': company_id, 'default_date_deadline': date_deadline, 'picking_type_code': picking_type_code, 'default_picking_id': id, 'form_view_ref': 'stock.view_stock_move_operations', 'address_in_id': partner_id, 'default_picking_type_id': picking_type_id, 'default_location_id': location_id, 'default_location_dest_id': location_dest_id, 'default_partner_id': partner_id}"
                        add-label="Add a Extras Move">
                        <list decoration-muted="scrapped == True or state == 'cancel' or (state == 'done' and is_locked == True)" string="Extras Moves" editable="bottom" create="0" delete="0">
                            <field name="name" column_invisible="True"/>
                            <field name="company_id" column_invisible="True"/>
                            <field name="state" readonly="0" column_invisible="True"/>
                            <field name="is_quantity_done_editable" column_invisible="True"/>
                            <field name="scrapped" column_invisible="True"/>
                            <field name="show_details_visible" column_invisible="True"/>
                            <field name="product_id" required="1" readonly="state != 'draft'" force_save="1"
                                options="{'no_create': True,'no_open': True}"
                                context="{'default_detailed_type': 'product'}"
                                domain="[('product_tmpl_id.division', '=', 'Extras')]"/>
                            <field name="description_picking" string="Description" optional="hide"/>
                            <field name="date" optional="hide"/>
                            <field name="date_deadline" optional="hide"/>
                            <field name="product_uom_qty" string="Quantity" readonly="1"/>
                            <field name="qty_available" string="Qty Available" readonly="1"/>
                            <field name="quantity" string="Qty Request" column_invisible="True"/>
                        </list>
                    </field>
                </field>
            </field>
        </record>
    </data>
</odoo>
