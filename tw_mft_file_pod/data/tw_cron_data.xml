<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="tw_ir_cron_auto_generate_mft_file_pod" model="ir.cron">
            <field name="name">Auto Generate MFT File POD</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="nextcall" eval="(DateTime.now().replace(hour=2, minute=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
            <field name="model_id" ref="model_tw_export_mft_file_pod"/>
            <field name="code">model.schedule_auto_generate_mft_file_pod()</field>
            <field eval="False" name="active"/>
            <field name="user_id" ref="base.user_root"/>
        </record>

    </data>
</odoo>
