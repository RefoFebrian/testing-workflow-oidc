<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Inherit Warehouse Form View -->
    <record id="view_warehouse_form_inherit_purchase_return" model="ir.ui.view">
        <field name="name">stock.warehouse.form.inherit.purchase.return</field>
        <field name="model">stock.warehouse</field>
        <field name="inherit_id" ref="stock.view_warehouse"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Purchase Return Operations" name="purchase_return_operations">
                    <group>
                        <field name="purchase_return_type_id" options="{'no_open': True, 'no_create_edit': True}"/>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
