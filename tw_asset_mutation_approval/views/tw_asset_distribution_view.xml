<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>  
    	
		<record id="tw_asset_distribution_approval_form_view" model="ir.ui.view">
            <field name="name">tw.asset.distribution.approval.form</field>
            <field name="model">tw.asset.distribution</field>
            <field name="inherit_id" ref="tw_asset_mutation.view_tw_distribution_asset_form"/>
            <field name="arch" type="xml">

				<field name="state" position="replace">
					<field name="state" widget="statusbar" statusbar_visible="requested,waiting_for_approval,approved,done"/>
				</field>

				<button name="action_confirm" position="replace">
					<button name="action_request_approval" invisible="state != 'requested'" string="RFA" type="object" class="oe_highlight" groups="tw_asset_mutation_approval.group_tw_asset_distribution_approval_request"/>
					<button name="action_approval" invisible="state != 'waiting_for_approval'" string="Approve" type="object" class="oe_highlight" />
					<button name="action_reject_or_cancel" invisible="state != 'waiting_for_approval'" string="Reject" type="object" context="{'window_title': 'Reject'}"/>
					<button name="action_confirm" invisible="state != 'approved'" data-hotkey="q" string="Confirm" type="object" class="oe_highlight" groups="tw_asset_mutation.group_tw_asset_distribution_confirm"/>
					<button name="action_reject_or_cancel" invisible="state not in ('waiting_for_approval', 'approved')" string="Cancel Approval" type="object" context="{'window_title': 'Cancel'}" groups="tw_asset_mutation_approval.group_tw_asset_distribution_approval_cancel"/>
				</button>

				<xpath expr="//page[@name='asset_detail']" position="after">
					<page string="Approval">
						<field name="approval_ids" readonly="True">
						</field>
					</page>
				</xpath>
			</field>
		</record>

		<record id="inherit_tw_asset_distribution_search_view" model="ir.ui.view">
			<field name="name">tw.asset.distribution.search.inherit</field>
			<field name="model">tw.asset.distribution</field>
			<field name="inherit_id" ref="tw_asset_mutation.view_teds_mutation_asset_filter"/>
			<field name="arch" type="xml">
				<xpath expr="//search" position="inside">
					<filter name="waiting_for_approval" string="Waiting for Approval" domain="[('state', '=', 'waiting_for_approval')]"/>
					<filter name="approved" string="Approved" domain="[('state', '=', 'approved')]"/>
				</xpath>
			</field>
		</record>
		
 	</data>
</odoo>